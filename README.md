## Define the PATHS

Use a defined url and path to the variable URL in the python files to run the program

1.  Make sure python is installed in your computer

2. install app required plugins to run the python files by using `pip3 reques matplotlib pandas`,

Note: Use pip3 if you have python3 installed , refer to the python version you have to run a command